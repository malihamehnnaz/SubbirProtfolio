Drop project images here and reference them from `src/lib/data.ts` as `/projects/trade-intercontinental/<filename>`.

Example:
- /projects/trade-intercontinental/hero.jpg
- /projects/trade-intercontinental/overview-1.jpg

Keep filenames lowercase and use dashes for spaces.