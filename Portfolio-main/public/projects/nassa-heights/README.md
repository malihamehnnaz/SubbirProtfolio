Drop project images here and reference them from `src/lib/data.ts` as `/projects/nassa-heights/<filename>`.

Example:
- /projects/nassa-heights/hero.jpg
- /projects/nassa-heights/amenities.jpg

Keep filenames lowercase and use dashes for spaces.