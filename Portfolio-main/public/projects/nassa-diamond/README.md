Drop project images here and reference them from `src/lib/data.ts` as `/projects/nassa-diamond/<filename>`.

Example:
- /projects/nassa-diamond/hero.jpg
- /projects/nassa-diamond/facade.jpg

Keep filenames lowercase and use dashes for spaces.